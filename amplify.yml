version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --prefix prototype
    build:
      commands:
        - npm run build --prefix prototype
  artifacts:
    baseDirectory: prototype/dist
    files:
      - '**/*'
  cache:
    paths:
      - prototype/node_modules/**/*
